import Presenter from './presenter.js';

/**
 * @extends {Presenter<EditorView>}
 */
class EditorPresenter extends Presenter {
  /**
   * @override
   */
  createViewState() {
    // TODO: EditorViewState
  }
}

export default EditorPresenter;
